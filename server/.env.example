# Database (SQLite for development)
DATABASE_URL="file:./dev.db"

# Server
PORT=3001
NODE_ENV=development

# Frontend URLs for CORS (comma-separated: store + admin)
FRONTEND_URL=http://localhost:8080,http://localhost:5174

# JWT secret for admin auth (use a long random string in production)
JWT_SECRET=your-super-secret-jwt-key-change-in-production

# Paystack secret key (for verifying payments server-side)
PAYSTACK_SECRET_KEY=sk_test_xxxxxxxx
